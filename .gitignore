# Rust build artifacts
/target/
frontend/target/
backend/target/

# macOS
.DS_Store

# SSH keys (should never be committed)
ssh-keys
ssh-keys.pub

# Claude
.claude/

# Trunk temp files (but keep dist/)
.trunk/
